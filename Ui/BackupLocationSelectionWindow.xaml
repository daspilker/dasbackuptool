<Window x:Class="DasBackupTool.Ui.BackupLocationSelectionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
    xmlns:ui="clr-namespace:DasBackupTool.Ui"
    Title="Backup Location Selection" SizeToContent="WidthAndHeight" ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
	<Window.CommandBindings>
		<CommandBinding Command="ui:Commands.RemoveBackupLocation" CanExecute="RemoveBackupLocationCanExecute"  Executed="RemoveBackupLocationExecuted"/>
	</Window.CommandBindings>
	<Window.InputBindings>
		<KeyBinding Command="ui:Commands.RemoveBackupLocation" Gesture="Delete"/>
	</Window.InputBindings>
	<Window.Resources>
		<CollectionViewSource x:Key="BackupLocations" Source="{Binding Path=BackupLocations}">
			<CollectionViewSource.SortDescriptions>
				<scm:SortDescription PropertyName="Path"/>
			</CollectionViewSource.SortDescriptions>
		</CollectionViewSource>
		<ui:FileIconValueConverter x:Key="FileIconValueConverter"/>
	</Window.Resources>

	<Grid Margin="12,12,12,12">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition MinWidth="180"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="6"/>
			<RowDefinition MinHeight="180"/>
			<RowDefinition Height="12"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="12"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3">Drop new backup locations here:</TextBlock>
		<ListView Name="BackupLocationsListView" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" ItemsSource="{Binding Source={StaticResource BackupLocations}}" AllowDrop="True" Drop="BackupLocationsListBoxDrop" ui:ListViewBehaviors.IsAutoUpdatingColumnWidths="true">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="HorizontalContentAlignment" Value="Stretch" />
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.View>
				<GridView>
					<GridView.Columns>
						<GridViewColumn Header="File Name">
							<GridViewColumn.CellTemplate>
								<DataTemplate>
									<StackPanel Orientation="Horizontal">
										<Image Source="{Binding Path=Path, Converter={StaticResource FileIconValueConverter}}" Margin="0,0,3,0" Width="16"/>
										<TextBlock Text="{Binding Path=Path}" ToolTip="{Binding Path=Path}"/>
									</StackPanel>
								</DataTemplate>
							</GridViewColumn.CellTemplate>
						</GridViewColumn>
						<GridViewColumn Header="Excluded">
							<GridViewColumn.CellTemplate>
								<DataTemplate>
									<CheckBox IsChecked="{Binding Path=Excluded}" HorizontalAlignment="Center"/>
								</DataTemplate>
							</GridViewColumn.CellTemplate>
						</GridViewColumn>
					</GridView.Columns>
				</GridView>
			</ListView.View>
			<ListView.ContextMenu>
				<ContextMenu>
					<MenuItem Command="ui:Commands.RemoveBackupLocation" Header="{Binding RelativeSource={RelativeSource Self}, Path=Command.Text}"/>
				</ContextMenu>
			</ListView.ContextMenu>
		</ListView>
		<Label Grid.Column="0" Grid.Row="4">Excluded files pattern:</Label>
		<TextBox Grid.Column="1" Grid.Row="4" Grid.ColumnSpan="2" Text="{Binding Path=ExcludedFilesPattern}"/>
		<StackPanel Grid.Column="2" Grid.Row="6" Orientation="Horizontal">
			<Button Margin="0,0,6,0" IsDefault="True" Click="OKButtonClick">OK</Button>
			<Button IsCancel="True">Cancel</Button>
		</StackPanel>
	</Grid>
</Window>
